# Website work: read first

For AI at Sea / David Lewis website tasks, read `AI-AT-SEA.md` and the relevant guides in `docs/ai-at-sea/` before changing anything. These instructions apply to this repository, which also contains unrelated projects; preserve those projects.

## Correct source and destination

- Repository: `Starman965/davidlewis`; production branch: `main`.
- Public website: https://www.davidlewis.ai/; hosting: existing GitHub Pages deployment.
- Retrieve the latest remote source before editing. Never reconstruct from chat memory, old uploaded code, or the separate ChatGPT Sites project.
- Commit `1322777` is the historical approved four-session baseline, NOT a standing instruction to roll back. Continue from latest main and preserve subsequent approved work.
- Verify source access and publishing capability. A new chat may need a fresh checkout; never assume a prior scratch path is available. If access is missing, explain the blocker without creating another site.

## Strategy and scope

- Primary website audience: cruise program directors, cruise directors, and speaker/enrichment agencies. Cruise guests are secondary readers and the audience for the sessions.
- The site helps buyers evaluate and book David's practical AI enrichment offering. It is not a general keynote/consulting funnel or attendee handout portal.
- Six standalone session choices, collaboratively selected for each sailing; not a required six-part series.
- Preserve established session text, imagery, layout and interactions unless David explicitly asks to change them. Latest explicit user instructions supersede these editorial defaults; update the guides when decisions change.
- Never fabricate credentials, clients, testimonials, bookings, or device capabilities.

## Implementation and delivery

- Follow `docs/ai-at-sea/WORKFLOW.md`; preserve unrelated files and concurrent edits.
- Use `docs/ai-at-sea/BRAND.md` for visual consistency and `STRATEGY.md` for messaging.
- Only publish when the user's task authorizes implementation/publication; discussion, diagnosis, and drafts do not authorize production writes.
- Use non-force updates. If main advances during work, reconcile against the new state rather than overwriting it.
- Verify the actual domain after publishing. Distinguish source checks, visual checks, and deployment checks; never claim a check that did not run.
- Keep lasting decisions and unresolved items in the guides, not only in a chat transcript. Do not record secrets or personal data unrelated to the website.
